What happens when you initialize a repository? Why do you need to do it?
When you create a new reposity Git creates a .git file that will control versions and commits. You need this to setup version control.

How is the staging area different from the working directory and the repository? What value do you think it offers?
In the staging area you may add only files that you changed.